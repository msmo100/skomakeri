# Kopiera den här filen till ".env.local" och fyll i värdet.
# .env.local ligger bara på din dator (den är gitignorerad).
#
# Mottagare för kontaktformuläret: mejladressen först, och efter aktiveringen
# hos formsubmit.co den hemliga koden i stället. Se README.md.
VITE_FORMSUBMIT_CODE=
